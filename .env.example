# Optional: leave empty to use local collection-note replies.
# Server-side only. Never add a real key to browser code or Git.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5
# Local development server port (default: 3000).
PORT=3000
