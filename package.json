{
  "name": "joyce-art-app",
  "version": "0.1.0",
  "private": true,
  "description": "The Art Historian's Common Room — an art history learning app with optional AI chat.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "setup": "node scripts/setup.cjs",
    "dev": "node scripts/dev-server.cjs",
    "check": "node --check script.js && node --check service-worker.js && node --check api/chat.js && node --check scripts/setup.cjs && node --check scripts/dev-server.cjs",
    "test": "node --test tests/*.test.cjs"
  }
}
